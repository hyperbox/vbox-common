apply plugin: 'java'
apply plugin: 'maven'

group = 'io.kamax'
version = 'latest'

sourceCompatibility = 1.7

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile 'io.kamax:hbox-api:latest'
    compile 'io.kamax:hbox-server-api:latest'
    compile 'io.kamax:hbox-vbox-common-api:latest'
}
